diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index abc123..def456 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@
+#include <vector>
+#include <utility>
+#include <cstdlib>
+
+namespace {
+    const std::vector<std::pair<const char*, const char*>> kWebGLFingerprintPool = {
+        {"NVIDIA Corporation", "NVIDIA GeForce RTX 3080/PCIe/SSE2"},
+        {"Intel Inc.", "Intel Iris Plus Graphics"},
+        {"AMD", "AMD Radeon RX 6800"},
+        {"Apple Inc.", "Apple M1 Pro"},
+        {"Qualcomm", "Adreno (TM) 650"},
+    };
+    std::pair<const char*, const char*> gSelectedFingerprint;
+    bool gWebGLFingerprintInitialized = false;
+
+    void InitializeWebGLFingerprint() {
+        if (!gWebGLFingerprintInitialized) {
+            int idx = rand() % kWebGLFingerprintPool.size();
+            gSelectedFingerprint = kWebGLFingerprintPool[idx];
+            gWebGLFingerprintInitialized = true;
+        }
+    }
+}

 const String WebGLRenderingContextBase::GetUnmaskedVendor() const {
-  return original_vendor_;
+  InitializeWebGLFingerprint();
+  return String(gSelectedFingerprint.first);
 }

 const String WebGLRenderingContextBase::GetUnmaskedRenderer() const {
-  return original_renderer_;
+  InitializeWebGLFingerprint();
+  return String(gSelectedFingerprint.second);
 }