diff --git a/third_party/blink/renderer/core/input/input_handler.cc b/third_party/blink/renderer/core/input/input_handler.cc
index 9abcdef..1234567 100644
--- a/third_party/blink/renderer/core/input/input_handler.cc
+++ b/third_party/blink/renderer/core/input/input_handler.cc
@@
+#include <cstdlib>
+#include <chrono>
+#include <thread>
+
+namespace {
+    void InjectRandomDelay() {
+        int jitter = rand() % 5; // 0–4 ms
+        if (jitter > 0) {
+            std::this_thread::sleep_for(std::chrono::milliseconds(jitter));
+        }
+    }
+}
 
 void InputHandler::HandleMouseEvent(const MouseEvent& event) {
+  InjectRandomDelay();
   // existing code
 }

 void InputHandler::HandleKeyboardEvent(const KeyboardEvent& event) {
+  InjectRandomDelay();
   // existing code
 }

 void InputHandler::HandleWheelEvent(const WheelEvent& event) {
+  InjectRandomDelay();
   // existing code
 }